services:
  php:
    image: php:8.2-cli
    container_name: training-git-php
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    command: php -S 0.0.0.0:8000